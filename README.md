# quizup-social (staging local)

Structure cible en 2 sous-modules Maven :

- `quizup-social-domain`
- `quizup-social-infrastructure`

Seul `quizup-social-domain` est importable inter-services.

Le code applicatif reste organise sous `io.github.quizup.social.application.*` dans le module infrastructure.

