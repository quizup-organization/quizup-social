server:
  port: 8087

spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/quizup_social
    username: postgres
    password: postgres

logging:
  level:
    io.github.quizup: DEBUG

